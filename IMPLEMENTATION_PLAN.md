# 구현 계획

## 제품 정의

Windows 11 무선 단말에서 WLAN 링크 상태, 내부망 다운로드 성능, 회사 프록시를 포함한 외부 사이트 다운로드 성능을 로컬에서 측정하고, 고정 규칙으로 장애 범위를 안내하는 Portable 진단 도구를 만든다.

## 마일스톤

### M0 — 저장소 기반

- 솔루션과 프로젝트 경계
- 개인정보·네트워크 통신 경계
- ADR, 위협 모델, 측정 방법
- 합성 데이터 자체 점검
- Windows CI 및 정적 감사

완료 기준:

- 빈 저장소에서 Windows CI가 빌드, 자체 점검, 감사를 통과한다.
- 프로그램 시작만으로 네트워크 연결을 만들지 않는다.
- 실제 사내 값과 민감 산출물이 저장소에 없다.

### M1 — WLAN 상태

- Native WLAN API 기반 인터페이스 열거
- 현재 연결 SSID/BSSID
- RSSI, Signal Quality
- 주파수, 채널, PHY 유형
- Rx/Tx PHY 링크 속도
- 연결 변화와 BSSID 변경 기록

완료 기준:

- 한국어·영어 Windows 출력 문자열 파싱 없이 동작한다.
- 위치 권한 거부와 무선 미연결 상태를 명확히 표시한다.

### M2 — 프록시 경로

- 현재 사용자 프록시 설정 확인
- 고정 프록시, 바이패스 목록
- PAC URL과 WPAD
- URL별 `PROXY`/`DIRECT` 결정
- 407 Negotiate/NTLM 처리
- 프록시 주소 기본 마스킹

완료 기준:

- 브라우저와 동일한 사용자 컨텍스트에서 대상 URL별 예상 경로를 표시한다.
- 프록시 서버에 관리자 접근 없이 동작한다.
- 프록시 내부 상태는 판단 범위에서 제외한다.

### M3 — 내부망 다운로드 측정

- 승인된 내부 HTTP/HTTPS 파일
- DIRECT 또는 프록시 바이패스 확인
- 최대 바이트·시간·리다이렉트 제한
- 평균 및 구간별 Mbps
- 내부 서버 성능 한계 경고

완료 기준:

- 다운로드 본문을 저장하지 않는다.
- 내부 URL이 프록시를 경유하면 내부망 기준 측정으로 인정하지 않는다.

### M4 — 외부망 다운로드 측정

- 사용자가 승인한 외부 정적 URL
- 현재 사용자 프록시 경유
- HEAD 사전 확인과 GET 다운로드
- DNS/TCP/TLS/TTFB/전체 시간
- HTTP 상태, 최종 URL, 캐시 관련 헤더
- 1스트림 기본, 최대 4스트림 선택
- 외부 업로드 기능 없음

완료 기준:

- 측정 요청 이외의 외부 통신이 없다.
- 복수 외부 대상의 편차를 개별 사이트와 공통 외부 경로로 구분한다.

### M5 — 브라우저 다운로드 관찰

- WLAN 인터페이스 Rx/Tx 카운터
- 0.5~1초 구간 처리량
- 측정 전 백그라운드 기준치
- RSSI/BSSID/PHY와 동시 기록

완료 기준:

- 프로그램이 외부 요청을 만들지 않는 관찰 모드를 제공한다.
- 다른 프로세스 트래픽이 섞일 수 있음을 신뢰도에 반영한다.

### M6 — 규칙과 보고서

- WLAN·내부·외부 결과 상관분석
- 근거 코드와 다음 점검 위치
- 로컬 JSON, CSV, 단일 HTML
- 외부 JavaScript·폰트·이미지 참조 없음

### M7 — Windows 배포

- win-x64 self-contained Portable ZIP
- 단일 EXE 보조 배포
- SHA-256
- 제3자 고지
- EDR/GPO/프록시 환경 수동 검증

## 우선순위

현재 작업 순서는 `M0 → M1 → M2 → M3 → M4 → M5 → M6 → M7`이다. 외부 다운로드 구현 전에 WLAN 수집과 프록시 경로 판정을 검증한다.
