# AGENTS.md

이 저장소에서 작업하는 사람과 자동화 도구는 아래 규칙을 우선 적용합니다.

## 제품 경계

1. 외부 통신은 사용자가 명시적으로 시작한 HTTP/HTTPS `HEAD` 또는 `GET` 측정만 허용합니다.
2. 외부 `POST`, `PUT`, `PATCH`, `DELETE`, WebSocket 전송 및 업로드 속도 측정을 구현하지 않습니다.
3. 텔레메트리, 자동 업데이트, 오류 자동 전송, 공인 IP·ISP·GeoIP 조회를 추가하지 않습니다.
4. AI, LLM, 로컬 모델, 클라우드 모델, 외부 분석 API를 추가하지 않습니다.
5. PCAP, 로그, WLAN 정보, 컴퓨터 이름, 사용자 이름, 내부 주소, 측정 결과를 외부로 전송하지 않습니다.
6. 브라우저 쿠키 저장소, 브라우저 Authorization 헤더 또는 저장된 웹 자격 증명을 읽지 않습니다.
7. 프록시 인증은 가능한 경우 현재 Windows 사용자 자격 증명과 WinHTTP의 OS 기능으로 처리하며 비밀번호를 저장하지 않습니다.
8. 프록시 서버 내부 상태를 확인한 것처럼 표현하지 않습니다.

## 구현 규칙

1. 런타임 NuGet 패키지를 추가하지 않습니다. 추가가 불가피하면 구현 전에 ADR과 라이선스 검토를 먼저 작성합니다.
2. 네이티브 핸들은 `SafeHandle` 또는 명확한 `finally` 블록으로 정리합니다.
3. WinHTTP가 할당한 메모리는 문서화된 해제 함수로 반드시 반환합니다.
4. URL은 최초 요청과 모든 리다이렉트 단계에서 다시 검증합니다.
5. `http`와 `https` 이외의 스킴을 거부합니다.
6. URL 사용자정보, 루프백·링크 로컬 대상 및 외부 모드의 사설 IP 리다이렉트를 거부합니다.
7. 실제 사내 프록시, PAC URL, 내부 도메인, SSID, BSSID, VLAN, 사용자 정보는 소스와 테스트 데이터에 넣지 않습니다.
8. GitHub Actions에서 실제 외부 사이트나 실제 사내 환경을 호출하지 않습니다.
9. 네트워크 테스트는 합성 로컬 서버와 모의 객체를 사용합니다.
10. 측정 결과의 단위는 명확히 구분합니다. PHY 링크 속도와 실제 처리량을 같은 값처럼 표시하지 않습니다.
11. 근거가 부족하거나 결과가 충돌하면 `판단 불가`로 처리합니다.
12. 기능 변경 시 관련 테스트, 문서, `IMPLEMENTATION_STATUS.md`, `CHANGELOG.md`를 함께 갱신합니다.

## 코드 품질

- 경고를 오류로 취급합니다.
- 비동기 작업은 취소 토큰과 상한 시간을 가져야 합니다.
- 측정 데이터는 불변 모델을 우선 사용합니다.
- 파싱과 판정은 같은 입력에서 같은 결과를 내야 합니다.
- 사용자 메시지는 쉬운 한국어로 작성하되 근거와 한계를 생략하지 않습니다.
- 제3자 저장소의 코드를 라이선스 확인 없이 복사하지 않습니다.
